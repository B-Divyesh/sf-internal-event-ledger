services:
  ledger:
    build:
      context: .
      args:
        BUILD_SHA: ${BUILD_SHA:-dev}
    ports:
      - "8080:8080"
    volumes:
      - ledger-data:/data
    deploy:
      replicas: 1
    restart: unless-stopped

volumes:
  ledger-data:
