[
  {
    "id": "demo-sandbox",
    "claim": "Try it with sample data opens an isolated ledger without an administrator token.",
    "where": "landing primary action, README demo section",
    "test": "npm run test:claims -- --grep @claim:demo-sandbox",
    "sandbox": "fresh 390px browser context; enter from the public landing page; assert seeded sources, events, reset, and no token prompt"
  },
  {
    "id": "demo-isolation",
    "claim": "Demo actions never read or change the operator's ledger.",
    "where": "demo banner, README demo section",
    "test": "npm run test:claims -- --grep @claim:demo-isolation",
    "sandbox": "fresh production database with a sentinel source; use demo without auth; assert only demo API calls and unchanged production events"
  },
  {
    "id": "demo-expiry",
    "claim": "Sample workspaces expire after 24 hours.",
    "where": "demo navigation, README demo section, .factory/demo.md",
    "test": "npm run test:claims -- --grep @claim:demo-expiry",
    "sandbox": "create a fresh server-side demo; assert its 86400-second deadline; advance its stored creation time by exactly 24 hours; assert the demo API returns the expired state"
  },
  {
    "id": "self-hosted-runtime",
    "claim": "The product starts as a self-hosted SQLite service with only PORT configured.",
    "where": "landing facts, README run and privacy sections",
    "test": "npm run test:claims -- --grep @claim:self-hosted-runtime",
    "sandbox": "fresh temporary directory; release binary receives only PATH and PORT; assert generated 256-bit token mode, SQLite file, health, and landing response"
  },
  {
    "id": "review-workflow",
    "claim": "Reviewers can search, acknowledge, archive, and produce a 24-hour digest from grouped events.",
    "where": "landing preview, README introduction and digest section",
    "test": "npm run test:claims -- --grep @claim:review-workflow",
    "sandbox": "fresh demo; search to two groups; acknowledge one; archive another; assert digest occurrence total"
  },
  {
    "id": "administrator-boundary",
    "claim": "Production management data requires the administrator token, which stays in the browser tab.",
    "where": "landing access form, privacy page, README",
    "test": "npm run test:claims -- --grep @claim:administrator-boundary",
    "sandbox": "fresh server and browser; assert anonymous management routes return 401, valid access works, and the token exists only in sessionStorage"
  },
  {
    "id": "retention-delete",
    "claim": "Running retention deletes event groups older than each source policy.",
    "where": "README digest, retention, and export section",
    "test": "npm run test:claims -- --grep @claim:retention-delete",
    "sandbox": "fresh database; create and age one fixture event beyond its one-day policy; run retention and assert exactly one deletion"
  },
  {
    "id": "response-policy",
    "claim": "HTML and the service worker revalidate while hashed assets use immutable caching.",
    "where": "README privacy and security section",
    "test": "npm run test:claims -- --grep @claim:response-policy",
    "sandbox": "fresh server; inspect root, hashed script, service-worker cache headers, and the response CSP"
  },
  {
    "id": "ledger-export",
    "claim": "Exports the complete sample ledger as CSV and JSON.",
    "where": "ledger toolbar, README",
    "test": "npm run test:claims -- --grep @claim:ledger-export",
    "sandbox": "fresh demo workspace; download both formats; assert CSV header plus one row per sample group and JSON record count"
  },
  {
    "id": "privacy-no-tracking",
    "claim": "The product loads no analytics, trackers, third-party fonts, or runtime CDN assets.",
    "where": "landing facts, privacy page, README",
    "test": "npm run test:claims -- --grep @claim:privacy-no-tracking",
    "sandbox": "record every browser request through landing, demo, and digest; assert every request is same-origin"
  },
  {
    "id": "offline-demo",
    "claim": "Sample events stay readable offline after one visit.",
    "where": "landing facts, README demo section",
    "test": "npm run test:claims -- --grep @claim:offline-demo",
    "sandbox": "fresh browser context; load /demo online; wait for service-worker control; set context offline; reload and assert all five groups"
  },
  {
    "id": "ingest-safety",
    "claim": "A source can require HMAC-SHA256, redact configured event fields and headers, and group repeated fingerprints.",
    "where": "README receiver and privacy sections",
    "test": "npm run test:claims -- --grep @claim:ingest-safety",
    "sandbox": "fresh database; create one signed source; send two signed fixture events; assert grouped count, redaction, and discarded credentials"
  },
  {
    "id": "receiver-token-once",
    "claim": "A source receiver token is shown once and each source has a separate token.",
    "where": "README receive events section, Sources screen",
    "test": "npm run test:claims -- --grep @claim:receiver-token-once",
    "sandbox": "fresh administrator browser session; create two sources; observe the first token, reload the Sources route, and assert it is no longer displayed and the tokens differ"
  },
  {
    "id": "receiver-authentication",
    "claim": "A source accepts its receiver token in the ledger header, Bearer header, or token query parameter.",
    "where": "README receive events section",
    "test": "npm run test:claims -- --grep @claim:receiver-authentication",
    "sandbox": "fresh source; post a fixture through each accepted token location and assert 202; post an invalid token and assert 401"
  },
  {
    "id": "group-state-transition",
    "claim": "A new event reopens an archived group and preserves an acknowledged group.",
    "where": "README receive events section",
    "test": "npm run test:claims -- --grep @claim:group-state-transition",
    "sandbox": "fresh source; archive then re-ingest one fingerprint and acknowledge then re-ingest another; assert unread and acknowledged states"
  },
  {
    "id": "health-identity",
    "claim": "GET /health returns service status and build SHA.",
    "where": "README digest, retention, and export section",
    "test": "npm run test:claims -- --grep @claim:health-identity",
    "sandbox": "fresh PORT-only server; assert a 200 JSON response contains status and a non-empty build identifier"
  },
  {
    "id": "receiver-quota",
    "claim": "Valid receiver deliveries have a separate quota from invalid delivery attempts.",
    "where": "README privacy and security section",
    "test": "npm run test:claims -- --grep @claim:receiver-quota",
    "sandbox": "fresh source and client address; exhaust invalid ingest attempts then assert a valid signed delivery is still accepted"
  },
  {
    "id": "scope-boundary",
    "claim": "The product is not a pager, retrying webhook proxy, automation engine, or guaranteed-delivery system.",
    "where": "README introduction, Terms scope section",
    "test": "npm run test:claims -- --grep @claim:scope-boundary",
    "sandbox": "fresh source; submit one event, wait through the retry window, and assert one stored event with no extra request or delivery activity"
  },
  {
    "id": "free-mit-license",
    "claim": "The product is free to self-host under the MIT License.",
    "where": "landing facts, README privacy and security section",
    "test": "npm run test:claims -- --grep @claim:free-mit-license",
    "sandbox": "fresh public landing browser context and repository license; assert the free self-host statement and MIT license text"
  },
  {
    "id": "self-hosted-controls",
    "claim": "The self-hosted ledger supports more than five sources, up to 10-year retention, and custom digest windows without an external service.",
    "where": "landing facts, sources, digest, settings, README",
    "test": "npm run test:claims -- --grep @claim:self-hosted-controls",
    "sandbox": "fresh SQLite database; create six 3650-day sources and request a six-hour digest; assert both succeed without a remote dependency"
  },
  {
    "id": "api-rate-limit",
    "claim": "Management and receiver API clients are rate limited and receive Retry-After when limited.",
    "where": "README security section",
    "test": "npm run test:claims -- --grep @claim:api-rate-limit",
    "sandbox": "one-replica SQLite deployment; send a concurrent 120-request burst from one forwarded client; assert one 60-token burst plus only the measured refill, 429, and Retry-After"
  }
]
